class AddCart {
  constructor() {
    this.add()
  }
  // 添加至购物车事件
  add() {
    // 先获取localStroage中的数据

    // 点击按钮时，该商品是否存在于数据中，存在在原有数据+1，不存在，新建一条初始化为1

    // 将数据渲染到购物车页和右上角购物车下拉菜单中

  }
}

new AddCart()
export default AddCart